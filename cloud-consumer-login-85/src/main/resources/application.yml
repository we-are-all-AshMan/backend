server:
  port: 85
spring:
  application:
    name: nacos-consumer-login
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

#消费者将要访问的微服务名称(注册成功进nacos的微服务提供者）
service-url:
  nacos-user-service: http://nacos-provider-login
  #提供者集群封装